{% extends 'base.html.twig' %}

{% block body %}

    <p>{{ quiz.name }}</p>

    {% if quiz.quizCodes|length > 0 %}
        {{ form(form) }}
            {% else %}
                <a href="{{ path('start_quiz', {'id': quiz.id, 'questionId': quiz.questions.first.id}) }}">Start quiz</a>
        {% endif %}

    {% endblock %}
